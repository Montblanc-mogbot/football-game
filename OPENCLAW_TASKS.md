# OPENCLAW_TASKS.md

## Project
Football Game (fresh-start MonoGame conversion of Tecmo Super Bowl NES)

## Branch / remote
- Branch: `automation/validate-2026-05-27-football-game`
- Remote: `origin`

## Reset rule
This repo starts fresh.
It contains only:
- the assembly snapshot under `reference/`
- the assembly-first planning/categorization notes created on 2026-05-26

Do not treat prior MonoGame implementation work as part of this repository.
If old code is ever consulted or imported from a separate repo, that must be an explicit future decision.

## Source of truth
- Original disassembly snapshot: `reference/Tecmo_Super_Bowl_NES_Disassembly/`
- Planning docs:
  - `docs/planning/conversion-inventory.md`
  - `docs/planning/bank-parity-status-matrix.md`
  - `docs/planning/critical-bank-conversion-packets.md`
  - `docs/planning/source-bank-conversion-checklist.md`
- Coding standards:
  - `docs/coding-standards.md`

## Active tasks
- [x] Convert `Bank21_22_play_commands_on_field_logic.asm` into a source-faithful inventory plus runtime-facing representation that accounts for the whole bank. Acceptance: represent all Bank21_22 content in durable extractor/docs/models/runtime ownership artifacts, use coherent production-facing runtime names, and explicitly connect the Bank21_22 runtime boundary to the existing Bank19_20 coordinator/holding-area work.
  - Evidence: added `docs/planning/banks/Bank21_22-structure-and-representation.md`, `development-tools/bank21_22/extract_bank21_22.py`, generated `content/game-data/bank21_22/generated/section-map.json`, generated `content/game-data/bank21_22/generated/summary.json`, typed conversion models/loaders under `src/FootballGame/Conversion/PlayCommands/`, and `docs/planning/validation/Bank21_22-conversion-validation.md`. Validation: `python development-tools/bank21_22/extract_bank21_22.py` succeeded, a follow-up assertion pass confirmed 164 extracted sections, 117 constants, the 12 group-command dispatch targets, and the four Bank19_20/Bank21_22 bridge jumps, and `git diff --check` passed.
  - Evidence: added `docs/planning/banks/Bank21_22-runtime-representation.md`, which defines production-facing `PlayerCommand*` runtime names for the Bank21_22 execution layer, ties them directly to `DO_NEXT_PLAYER_COMMAND`, and anchors the current Bank19_20 integration seam to `PlayAssignmentService`, `PreSnapControlService`, `PassTargetingService`, `OnFieldRoutineOwnershipMap`, and `CommandRuntimeBoundaryHoldingArea` with exact source/code references.
- [x] Packet 21A — offensive command semantics slice. Acceptance: identify one bounded offensive command family in `Bank21_22_play_commands_on_field_logic.asm`, define its runtime meaning with source references, and show how it should be entered/stepped from the current Bank19_20 host flow.
  - Evidence: added `docs/planning/banks/Bank21_22-packet-21A-snap-exchange-and-backfield-transfer-semantics.md`, which documents the snap-exchange and immediate backfield-transfer family (`CENTER_HIKE_COMMAND_START`, `SHOTGUN_HIKE_COMMAND_START`, `RECEIVE_SNAP_*`, `HANDOFF_COMMAND_START`, `FAKE_HANDOFF_COMMAND_START`, `PITCH_BALL_COMMAND_START`, `RB_RECEIVES_HANDOFF_START`, `WAIT_FOR_PLAYER_RECEIVES_PITCH`) and traces the current Bank19_20 entry/step boundary through `LOAD_UPDATE_PLAY_CODE_FUNCTIONS`, `DEFENDER_CHANGE_BEFORE_HIKE`, `CHECK_SNAP_PUNT`, `SET_BALL_SNAPPED_START_CLOCK_EXCEPT_XP`, and `SET_PLAYERS_CLOSE_TO_PASS`.
- [ ] Packet 21B — defensive reaction semantics slice. Acceptance: identify one bounded defensive reaction family in `Bank21_22_play_commands_on_field_logic.asm`, define its runtime meaning with source references, and show how it should connect to the current Bank19_20 host/runtime boundary.
- [ ] Packet 21C — interaction edge-case semantics slice. Acceptance: identify one command-driven interaction edge case in `Bank21_22_play_commands_on_field_logic.asm`, define the runtime semantics with source references, and call out any Bank19_20 bridge assumptions that must remain explicit.
- [x] Review the actual Bank19_20 runtime coordinator/state/services from direct code inspection and document the findings in-repo. Acceptance: leave a durable review note focused only on the code currently present under `src/FootballGame/Gameplay/OnField/`, including what the coordinator mechanically does, what the services really own today, and what remains shallow or risky.
  - Evidence: added `docs/reviews/Bank19_20-runtime-code-review-2026-06-03.md`, which reviews `OnFieldPlayCoordinator`, `OnFieldGameState`, `OnFieldRoutineOwnershipMap`, the on-field services, and the command-runtime bridge holder directly from current code rather than from planning docs.
- [x] Review the full Bank19_20 implementation slice and document every type introduced during this bank. Acceptance: leave one durable review note summarizing the bank from inventory/extraction through runtime/audit work, plus one durable type reference covering the conversion/runtime classes and related Bank19_20 types.
  - Evidence: added `docs/reviews/Bank19_20-implementation-review-2026-06-03.md`, which summarizes the full bank from extractor/inventory work through runtime slices, audits, and parity fixes, and added `docs/planning/banks/Bank19_20-type-reference.md`, which documents the conversion-layer records/enums, runtime host/state types, supporting services, and Bank21_22 bridge type introduced during the Bank19_20 work.
- [x] Validate the current working tree on `automation/validate-2026-05-27-football-game` and commit/push if green. Scope: inspect the current uncommitted `docs/reviews/Bank19_20-football-rules-review-2026-06-02.md` slice, run `git diff --check`, commit only if the tree is coherent and validation passes, then push and update this task with evidence. Acceptance: either (a) committed + pushed with validation evidence, or (b) task updated with the exact blocker preventing commit.
  - Evidence: confirmed the working tree was limited to `docs/reviews/Bank19_20-football-rules-review-2026-06-02.md` plus its matching `OPENCLAW_TASKS.md` task entry, `git diff --check` passed, and the docs-only slice was committed/pushed on `automation/validate-2026-05-27-football-game` as `4e695bb docs(reviews): add Bank19_20 football rules review`.
- [x] Validate the current working tree on `automation/validate-2026-05-27-football-game` and commit/push if green. Scope: inspect the current uncommitted Bank19_20 review-doc slice, run `git diff --check`, verify the tree is limited to `docs/reviews/Bank19_20-code-quality-review-2026-06-02.md` plus the matching `OPENCLAW_TASKS.md` evidence/task updates, commit only if the slice is coherent and validation passes, then push and update this task with evidence. Acceptance: either (a) committed + pushed with validation evidence, or (b) task updated with the exact blocker preventing commit.
  - Blocker: there is no longer any uncommitted Bank19_20 review-doc slice to validate or commit on `automation/validate-2026-05-27-football-game`. Evidence: `git status --short` returned clean output, `git diff --name-only` returned nothing, and `git diff --check` passed against an empty working tree; branch status shows `automation/validate-2026-05-27-football-game...origin/automation/validate-2026-05-27-football-game`, so there was nothing to commit or push for this task.
- [x] Run a Bank19_20 code-quality review focused on stability, principledness, and coding-standards adherence. Acceptance: review the current Bank19_20 MonoGame coordinator/service implementation against `docs/coding-standards.md`, identify any stability or design-principle concerns, and return concrete findings with file-level references.
  - Evidence: added `docs/reviews/Bank19_20-code-quality-review-2026-06-02.md`, which records five concrete findings with file-level references. Main issues: `OnFieldPlayCoordinator` is accumulating god-class scope; `AdvanceActivePlayPhase` checks `PlayType` before `TurnoverReturnActive`, which can misroute active return flow; `ApplyPossessionChange` duplicates routine recording already owned by `StatAccountingService`; multiple Bank19_20 services still act as silent placeholders without invariant checks; and stringly-typed formation/banner/cutscene keys are spreading through runtime state.
- [x] Run a Bank19_20 football-rules review and coordinator flow-chart pass. Acceptance: review the current Bank19_20 coordinator logic for football-rules fidelity, identify any likely gameplay/rules mistakes or questionable transitions, and produce a flow chart documenting how the coordinator currently operates.
  - Evidence: added `docs/reviews/Bank19_20-football-rules-review-2026-06-02.md`, which includes a coordinator flow chart plus six concrete football-rules/parity findings. Main issues: interception returns can be misrouted because `AdvanceActivePlayPhase` checks `PlayType` before `TurnoverReturnActive`; same-team fumble recoveries can stay pseudo-live too long; blocked punts still collapse into an immediate possession change instead of a live-ball outcome; missed field goals omit the source’s inside-the-20 spot adjustment; safety kickoff ownership appears inverted; and XP exit handling currently models kickoff setup as a possession change.
- [x] Verify Bank19_20 assembly presence against the JSON manifest and MonoGame implementation before audit. Acceptance: compare the assembly sections to `content/game-data/on-field/generated/bank19_20-section-map.json` and then compare the manifest to the MonoGame conversion/runtime ownership artifacts so we can confirm what is present versus missing before a deeper sanity/bug audit.
  - Evidence: a comparison script confirmed `Bank19_20_on_field_gameplay_loop.asm` exposes 75 section markers and the generated JSON manifest contains the same 75 sections with no assembly↔manifest drift; all 75 manifest sections are present in `OnFieldRoutine` and `OnFieldRoutineOwnershipMap`; no coordinator-owned routine remains absent from `OnFieldPlayCoordinator`; and the conversion inventory layer still carries the manifest’s 19 script-pointer families plus 11 external dependencies via the JSON loader/inventory records.
- [x] Finish converting the remaining shallow coordinator-owned Bank19_20 routines before the next audit pass. Acceptance: systematically identify any Bank19_20 routines still represented only weakly/indirectly in `OnFieldPlayCoordinator`, implement the highest-value remaining host-flow conversions so the bank is more fully gathered into the runtime before another audit pass, validate the slice, and push it for review.
  - Evidence: tightened `ResolveSafetyOutcome` to explicitly record `P1_SAFETIED` / `P2_SAFETIED`, and tightened `ApplyPossessionChange` to explicitly record `CHECK_FOR_FIRST_DOWN_OR_TOD` plus `UPDATE_HASHMARK_FOR_NEXT_SNAP` in the coordinator trace itself. Validation: a follow-up coverage script confirmed that no `OnFieldPlayCoordinator.CoveredRoutines` entry remains represented only by the covered-routines list; `git diff --check` passed.
- [x] Audit Bank19_20 coordinator ownership coverage against the runtime map. Acceptance: compare the current `OnFieldPlayCoordinator` behavior against the Bank19_20 ownership/runtime representation so any still-shallow or disconnected coordinator-owned routine coverage is made more explicit, validate the slice, and push it for review.
  - Evidence: added `RecordReturnOutcomeChecks` and wired it into kickoff, punt, interception, onside, and fumble return paths so the coordinator now explicitly records the shared Bank19_20 return outcome-evaluator family (`CHECK_FOR_TD`, `CHECK_FOR_FUMBLES_TOSS_AND_NORMAL`, `CHECK_FOR_PLAY_OVER`) instead of relying only on state flags. Validation: targeted assertions confirmed the helper and all of its live-flow call sites are present; `git diff --check` passed.
- [x] Harden Bank19_20 dead-ball and quarter-end transition cleanup. Acceptance: audit the current coordinator logic for inconsistent dead-ball finalization across normal play, turnovers, special teams, and restart paths; implement the highest-value cleanup so task teardown / quarter-over checks / next-sequence dispatch are less fragmented; validate the slice, and push it for review.
  - Evidence: added a shared `FinalizeDeadBallTransition` helper and routed normal play-over, made field goals, extra-point exit, safety restart, same-team fumble recovery, and other restart paths through `QueueNextPlayOrKickoffState` so Bank19_20 task teardown, quarter-over recording, transient dead-ball flag cleanup, and next-sequence dispatch are less fragmented. Validation: targeted assertions confirmed the shared finalization helper and queue-based restart routing are present; `git diff --check` passed.
- [x] Harden Bank19_20 blocked-kick, onside, and quarter-end edge cases. Acceptance: audit the current coordinator logic for blocked field-goal aftermath, onside live-return flow, and remaining possession-change / quarter-end edge cases that are still under-specified; implement the most important fixes, validate the slice, and push it for review.
  - Evidence: upgraded blocked field-goal handling from a placeholder event into explicit loose-ball/recovery routing using the existing recovery helpers, and corrected onside return scoring to use `OnFieldTouchdownKind.SpecialTeamsReturn`. Validation: targeted assertions confirmed the blocked-FG loose-ball path, live return reassignment, and special-teams touchdown classification are present; `git diff --check` passed.
- [x] Harden Bank19_20 scoring and special-teams edge-case routing. Acceptance: audit the current coordinator logic for XP/FG aftermath, kickoff/onside dead-ball transitions, and other special-teams/scoring edge cases that are still under-specified; implement the most important fixes, validate the slice, and push it for review.
  - Evidence: added `OnFieldTouchdownKind.SpecialTeamsReturn`; updated `RunKickoffFlow` and `RunPuntFlow` so kickoff/punt return flow now waits for dead-ball resolution before resolving possession/next-play setup, with touchdown/safety return exits represented explicitly; added `ResolveExtraPointExitToKickoff` so made/missed/blocked extra-point outcomes now all transition into kickoff flow rather than dead-ending. Validation: targeted assertions confirmed the new XP exit helper, special-teams return touchdown routing, and dead-ball wait states are present; `git diff --check` passed.
- [x] Harden the current Bank19_20 coordinator slice for parity and code health. Acceptance: audit the current `OnFieldPlayCoordinator`/service implementation for source-trace alignment issues, duplicated or inconsistent possession-change routing, invalid or unintegrated host methods, and workspace noise; implement the most important fixes, validate the slice, and push it for review.
  - Evidence: wired kickoff progression into `AdvanceActivePlayPhase` through new `RunKickoffFlow` handling, reset kickoff host-state at `StartPlayer1Kickoff` / `StartPlayer2Kickoff`, replaced the stale interception branch in `ResolvePassOutcome` with `HandleInterceptionResult`, normalized punt touchback/return and blocked-punt routing through `ApplyPossessionChange` + `QueueNextPlayOrKickoffState`, and removed generated `__pycache__` noise from the repo. Validation: targeted assertions confirmed the kickoff route and interception handoff are present and the stale interception reassignment path is gone; `git diff --check` passed.
- [x] Implement the remaining Bank19_20 turnover/score/special-outcome host flow slice. Acceptance: trace the remaining coordinator-owned interception, touchdown, onside, and fumble/recovery source routines, implement the corresponding `OnFieldPlayCoordinator` logic with supporting service updates as needed, validate the slice, and push it for review.
  - Evidence: traced the remaining Bank19_20 interception (`P1/P2_INTERCEPTED`), touchdown (`P1/P2_TD`), onside (`P1/P2_ONSIDES_RETURN`), and loose-ball/fumble (`CHECK_FOR_FUMBLES_TOSS_AND_NORMAL`, `ONSIDE_AND_FUMBLE_RECOVERY_LOGIC`, `P1/P2_RECOVERS_FUMBLE`, `MISC_FUMBLE_FUNCTIONS`) families; added `OnFieldTouchdownKind`; expanded `OnFieldGameState` with recovery/turnover-return state; extended `PlayAssignmentService`, `OnFieldPresentationService`, `StatAccountingService`, and `InjuryCutsceneService`; and extended `OnFieldPlayCoordinator` with explicit host methods for interception return, touchdown aftermath, onside recovery, loose-ball recovery, turnover series reset, and next-play/kickoff routing. Validation: targeted assertions confirmed the new coordinator methods and turnover-reset hooks are present, `git diff --check` passed, and the slice was committed/pushed as `43b8340`.
- [x] Implement the Bank19_20 punt/field-goal host flow slice. Acceptance: trace the P1/P2 punt and FG/XP source routines, implement the next real `OnFieldPlayCoordinator` logic for special-teams progression beyond pre-snap with supporting service updates as needed, validate the slice, and push it for review.
  - Evidence: added `OnFieldKickOutcome`, expanded `OnFieldGameState` with kick/return/touchback/cutscene state, updated `PlayAssignmentService`, `PlayerSkillHydrationService`, and `OnFieldPresentationService`, and extended `OnFieldPlayCoordinator` with `RunPuntFlow`, `RunFieldGoalOrExtraPointFlow`, `ResolveSpecialTeamsPlayOver`, `ResolveBlockedPunt`, `ResolveMadeFieldGoalOrExtraPoint`, `ResolveMissedFieldGoalOrExtraPoint`, and `ResolveBlockedFieldGoalOrExtraPoint` so the Bank19_20 host now represents punt return setup, touchback routing, field-goal/XP made-missed-blocked routing, and special-teams possession-change handling. Validation: follow-up assertions confirmed the new coordinator methods and supporting service calls are present, and `git diff --check` passed.
- [x] Implement the Bank19_20 pass/sack/no-throw host flow slice. Acceptance: trace the P1/P2 pass-play and sack-or-scramble source routines, implement the next real `OnFieldPlayCoordinator` logic for pass lifecycle and no-throw/sack outcomes with supporting service updates as needed, validate the slice, and push it for review.
  - Evidence: added `OnFieldPassOutcome`, expanded `OnFieldGameState` with pass-lifecycle and sack/outcome flags, updated `PassTargetingService`, `OnFieldPresentationService`, and `InjuryCutsceneService`, and extended `OnFieldPlayCoordinator` with `RunPassPlayLoop`, `WaitForQuarterPassFlight`, `TransitionPassPlayToScramble`, `ResolvePassOutcome`, `ResolveIncompletePass`, `ResolvePassPlayOverNoThrow`, and `ResolveQuarterbackSackOutcome` so the Bank19_20 host now represents pass targeting, pass-flight staging, interception/tip/incomplete routing, and sack/no-throw outcomes. Validation: follow-up assertions confirmed the new coordinator methods and service calls are present, and `git diff --check` passed.
- [x] Implement the next Bank19_20 host-flow slice after kickoff/play-select. Acceptance: trace the next source-faithful coordinator responsibilities after the existing kickoff/play-start entrypoints, implement the next real `OnFieldPlayCoordinator` logic slice with supporting service updates as needed, validate the new flow coverage, and push it for review.
  - Evidence: extended `OnFieldPlayCoordinator` with `TransitionFromPlaySelection`, `StartRegularPlay`, `StartRunPlay`, `StartPassPlay`, `StartSpecialTeamsPlay`, `StartPuntPlay`, `StartFieldGoalPlay`, `ResolveNormalPlayOver`, `ResolveTurnoverOnDowns`, and `ResolveSafetyOutcome`, then updated `PreSnapControlService`, `OnFieldPresentationService`, `PlayAssignmentService`, `StatAccountingService`, `InjuryCutsceneService`, `OnFieldGameState`, and `OnFieldPhase` so the Bank19_20 host now represents regular-play routing, pre-snap setup, run/pass start selection, punt/FG/XP host entry, and play-over/possession-change resolution. Validation: follow-up assertions confirmed the new coordinator methods and service calls are present, and `git diff --check` passed.
- [x] Rename the awkward Bank19_20 runtime mapping names and implement the first real coordinator logic slice. Acceptance: replace the generic `Bank19SectionName`/`Bank19Runtime...` naming with clearer gameplay-facing names, implement real `OnFieldPlayCoordinator` entry/play-start logic instead of leaving it empty, validate the new coverage, and push the result for review.
  - Evidence: renamed the runtime-facing mapping types to `OnFieldRoutine`, `OnFieldOwnerKind`, `OnFieldRoutinePlacement`, `OnFieldRoutineOwnershipMap`, and `CommandRuntimeBoundaryHoldingArea`; added `OnFieldGameState`, `OnFieldTeam`, `OnFieldPhase`, `OnFieldPlayType`, and `OnFieldKickoffStrategy`; and implemented the first source-traceable coordinator logic slice in `OnFieldPlayCoordinator` covering `GAME_PLAY_START_CHECK_FOR_KICK_TEAM`, `P1_KICKOFF`, `P2_KICKOFF`, and the two play-select/load paths with real service calls into play assignment, skill hydration, task coordination, presentation, and CPU kickoff strategy selection. Validation: follow-up assertions confirmed the renamed runtime map still covers every Bank19_20 routine exactly once and the coordinator contains the new kickoff/play-start logic; `git diff --check` passed.
- [x] Represent all Bank19_20 content in runtime-facing code. Acceptance: add source-traceable Bank19_20 coordinator/service skeletons so every Bank19_20 section is accounted for in either the coordinator, a Bank19_20 service, or an explicit Bank21_22 holding-area artifact for later runtime work; validate coverage and push the result for review.
  - Evidence: added `src/FootballGame/Gameplay/OnField/OnFieldPlayCoordinator.cs`, `src/FootballGame/Gameplay/OnField/Services/*.cs`, `src/FootballGame/Gameplay/OnField/OnFieldRoutineOwnershipMap.cs`, `src/FootballGame/Gameplay/OnField/OnFieldRoutine.cs`, `src/FootballGame/Gameplay/OnField/OnFieldRoutinePlacement.cs`, `src/FootballGame/Gameplay/OnField/OnFieldOwnerKind.cs`, and `src/FootballGame/Gameplay/OnField/CommandRuntimeBridge/CommandRuntimeBoundaryHoldingArea.cs`, plus `docs/planning/banks/Bank19_20-runtime-representation.md`. Validation: a follow-up assertion pass confirmed every extracted Bank19_20 section is represented exactly once in the runtime placement map, the combined coordinator/service covered-section lists match the full section set, and the four Bank19_20-to-Bank21_22 bridge sections are mirrored into the holding area; `git diff --check` passed.
- [x] Continue the full Bank19_20 conversion beyond architecture notes. Acceptance: identify the remaining Bank19_20 conversion artifacts still missing compared with the earlier full-bank passes, implement the next concrete artifact(s) rather than only additional architecture notes, validate them, and push the continuation for review.
  - Evidence: identified the missing Bank19_20 loader/semantic bridge, then added `src/FootballGame/Conversion/OnField/Bank19OnFieldGameplayInventoryJsonLoader.cs`, `src/FootballGame/Conversion/OnField/Bank19ExternalJumpConstantRecord.cs`, `src/FootballGame/Conversion/OnField/Bank19SectionLabelRecord.cs`, updated the aggregate/section records to preserve `externalJumpConstants` plus section label records, and added `docs/planning/banks/Bank19_20-loader-layer.md`. Validation: a follow-up assertion pass confirmed the loader maps `ExternalJumpConstants`, section `Labels`, and the explicit enum parsers, and `git diff --check` passed.
- [x] Build a Bank19_20 script-pointer and reassignment matrix. Acceptance: add one durable note that catalogs the special script-pointer families in `Bank19_20_on_field_gameplay_loop.asm`, traces the main call sites that load or retarget them, links each trigger/event to the likely MonoGame service boundary, and explicitly calls out any transitions that must be carried forward into Bank21_22 runtime work.
  - Evidence: added `docs/planning/banks/Bank19_20-script-pointer-and-reassignment-matrix.md`, which inventories the 19 special script-pointer families, maps their main kickoff/punt/onside/interception/fumble/touchdown/chase-ball-carrier reassignment triggers, explains the `UPDATE_ALL_*` vs `_EXCEPT_MAN` policy pattern, ties the behavior to a future `PlayAssignmentService`, and reinforces the Bank19_20-to-Bank21_22 carry-forward bridge set.
- [x] Complete the Bank19_20 conversion slice using the current inventory/architecture work. Acceptance: add the missing durable Bank19_20 conversion artifacts needed to represent the bank faithfully, explicitly mark any routines/content that should be carried forward into the Bank21_22 runtime notes, validate the artifacts, and push the result for review.
  - Evidence: added `docs/planning/banks/Bank19_20-structure-and-representation.md`, `development-tools/bank19_20/extract_bank19_20.py`, generated `content/game-data/on-field/generated/bank19_20-section-map.json`, generated `content/game-data/bank19_20/generated/summary.json`, typed conversion models under `src/FootballGame/Conversion/OnField/`, and `docs/planning/validation/Bank19_20-conversion-validation.md`. Also updated `docs/planning/banks/Bank21_22-architecture-review.md` to carry forward the Bank19_20-to-Bank21_22 bridge sections (`LOAD_UPDATE_PLAY_CODE_FUNCTIONS`, `DEFENDER_CHANGE_BEFORE_HIKE`, `CHECK_SNAP_PUNT`, `SET_PLAYERS_CLOSE_TO_PASS`). Validation: `python development-tools/bank19_20/extract_bank19_20.py` succeeded, a follow-up Python assertion pass confirmed 75 extracted sections, 19 script-pointer families, 2 entrypoints, and 4 Bank21_22 carry-forward sections, and `git diff --check` passed.
- [x] Inventory `Bank19_20_on_field_gameplay_loop.asm` and classify its responsibilities. Acceptance: add one durable note that inventories the major sections/routines in Bank19_20, marks which ones belong to the future on-field controller versus supporting services/state systems versus other layers, and calls out any areas where the assembly host is doing more than one modern responsibility at once.
  - Evidence: added `docs/planning/banks/Bank19_20-inventory-and-responsibility-map.md`, including section-by-section ownership for Bank19_20 entry/phase routing, play-end adjudication, pre-snap control, script assignment helpers, CPU play logic, pass-targeting helpers, stats, injury, cutscene, audio, and field-presentation systems.
- [x] Review the coding standards before locking in the Bank19_20 / Bank21_22 runtime shape, then sketch the likely MonoGame classes. Acceptance: add one durable note that checks the proposed coordinator/interpreter/handler split against `docs/coding-standards.md`, calls out any architectural guardrails from those standards, and sketches the likely classes/responsibilities for the on-field host and per-player script runtime.
  - Evidence: added `docs/planning/banks/Bank19_20-and-Bank21_22-monogame-class-sketch.md`, which checks the coordinator/interpreter/handler split against the repo coding standards, calls out the standards' guardrails around layer separation and source-bank traceability, and sketches likely classes such as `OnFieldPlayCoordinator`, `PlayAssignmentService`, `PlayerScriptRunner`, `ScriptCursor`, `ActiveInstructionState`, and `ScriptInstructionDispatcher`.
- [x] Synthesize the gameplay runtime architecture across `Bank17_18_main_game_loop.asm`, `Bank19_20_on_field_gameplay_loop.asm`, and `Bank21_22_play_commands_on_field_logic.asm`. Acceptance: add one durable note that explains which bank owns match/session flow versus on-field host logic versus per-player command execution, and explicitly describes how and where Bank5_6 reaction scripts are assigned, entered, stepped, and resumed.
  - Evidence: added `docs/planning/banks/Bank5_6-consumption-architecture.md`, which frames Bank17_18 as match/session orchestration, Bank19_20 as the live on-field host and script-assignment/reassignment layer, Bank21_22 as the per-player command runtime, and explicitly traces how Bank5_6 reaction scripts are copied into `PLAY_CODE_ADDR`, entered through `DO_NEXT_PLAYER_COMMAND`, and retargeted during turnovers, returns, and special-teams transitions.
- [x] Implement the first Bank5_6 source-faithful extractor slice. Acceptance: add `development-tools/bank5_6/extract_bank5_6.py`, generate `content/game-data/play-scripts/generated/bank5_6-play-scripts.json` plus `content/game-data/bank5_6/generated/summary.json`, preserve offense/defense reaction ordering plus label/control-flow metadata, and leave validation evidence tying the generated counts/targets back to the source bank.
  - Evidence: added `development-tools/bank5_6/extract_bank5_6.py`, generated `content/game-data/play-scripts/generated/bank5_6-play-scripts.json`, and generated `content/game-data/bank5_6/generated/summary.json`. Validation: `python development-tools/bank5_6/extract_bank5_6.py` succeeded, the summary confirms 457 offense reactions and 522 defense reactions, and a follow-up Python assertion pass verified representative anchors including `OFFENSE_PLAYER_REACTION_091` beginning with `takeSnapUnderCenter` and jumping to `OFFENSE_PLAYER_REACTION_314`, plus `DEFENSE_PLAYER_REACTION_001` jumping to `DEFENSE_PLAYER_REACTION_191`.
- [x] Design the Bank5_6 extractor and intermediate-representation layer. Acceptance: add one durable note that defines the extraction pipeline, the source-faithful script artifact shape, the semantic instruction layer, how labels/jumps/loops/branch destinations should be preserved, and what runtime-owned state must stay out of the extracted asset.
  - Evidence: added `docs/planning/banks/Bank5_6-extractor-and-ir-design.md`, which defines a staged Bank5_6 extraction pipeline, a source-faithful reaction-script artifact shape, a semantic instruction/label/jump-target IR, explicit label-based control-flow preservation rules, validation invariants, and a clear boundary keeping live runtime state out of the extracted assets.
- [x] Validate the current working tree on `automation/validate-2026-05-27-football-game` and commit/push if green. Scope: inspect the current uncommitted Bank5_6 planning-doc changes, run `git diff --check`, verify the slice is limited to `docs/planning/banks/Bank5_6-structure-and-representation.md`, `docs/planning/banks/Bank5_6-script-reference.md`, and the matching `OPENCLAW_TASKS.md` evidence updates, commit only if the tree is coherent and the check passes, then push and update this task with evidence. Acceptance: either (a) committed + pushed with validation evidence, or (b) task updated with the exact blocker preventing commit.
  - Evidence: `git diff --check` passed before commit, and the doc batch was committed as `090f564 docs(planning): add bank 5_6 and 17_18 review notes` and pushed to `origin/automation/validate-2026-05-27-football-game`. By commit time the slice had grown slightly to include the related `docs/planning/banks/Bank17_18-architecture-review.md` note, but the working tree remained coherent and docs-only.
- [x] Review `Bank17_18_main_game_loop.asm` for architecture-bearing gameplay-loop responsibilities. Acceptance: add one durable note summarizing the main loop/state-transition responsibilities in Bank17_18, what is NES/platform plumbing versus real gameplay orchestration, and which responsibilities likely shape the future MonoGame architecture.
  - Evidence: added `docs/planning/banks/Bank17_18-architecture-review.md`, which frames Bank17_18 as the high-level match/session orchestration layer, calls out season-vs-in-game state transfer, quarter/halftime/overtime flow, clock control, skip-mode routing, and separates those responsibilities from NES/UI plumbing like PPU/IRQ/MMC3 details.
- [x] Review `Bank21_22_play_commands_on_field_logic.asm` for architecture-bearing command-runtime responsibilities. Acceptance: add one durable note summarizing the command-dispatch/runtime responsibilities in Bank21_22, especially per-player execution state, command scheduling, cross-player coordination, and what this implies for a future MonoGame gameplay runtime.
  - Evidence: added `docs/planning/banks/Bank21_22-architecture-review.md`, which frames Bank21_22 as the per-player command interpreter/runtime, documents `DO_NEXT_PLAYER_COMMAND` decode/dispatch behavior, multi-frame resumable command execution, control-flow and CPU-conditional semantics, and the likely split between script assets, semantic instructions, and runtime execution services.
- [x] Define the formal Bank5_6 structure-and-representation strategy. Acceptance: add one durable bank note that explains the top-level offense/defense script-bank structure, the script-graph and command-language invariants that must survive conversion, the split between source-faithful extracted artifacts and later semantic/runtime layers, and what should not be preserved literally from the NES implementation.
  - Evidence: added `docs/planning/banks/Bank5_6-structure-and-representation.md`, which frames Bank5_6 as a behavior-script source bank, documents the offense/defense script split and script-graph invariants, defines the extracted/semantic/runtime representation split, and explicitly defers final MonoGame runtime architecture until the main gameplay loop is in place.
- [x] Draft a Bank5_6 script reference doc that lists the command vocabulary and explains how the reaction-script bank is consumed at runtime. Acceptance: add one durable note that covers the offense/defense reaction-bank structure, player-slot vocabulary, command groups/opcodes, and the current understanding of how the gameplay runtime executes these scripts without prematurely locking in the MonoGame architecture.
  - Evidence: added `docs/planning/banks/Bank5_6-script-reference.md`, which documents the offense/defense reaction-bank split, the shared player-slot nibble vocabulary, the macro/opcode command families from `play_data_macros.asm`, and the current runtime-consumption model traced through `Bank19_20_on_field_gameplay_loop.asm` and `Bank21_22_play_commands_on_field_logic.asm`.
- [x] Validate the current working tree on `automation/validate-2026-05-27-football-game` and commit/push if green. Scope: inspect the current uncommitted Bank4 defensive-play conversion changes, run `python development-tools/bank4/extract_bank4.py && dotnet build`, commit only if the tree is coherent and validation passes, then push and update this task with evidence. Acceptance: either (a) committed + pushed with validation evidence, or (b) task updated with the exact blocker preventing commit.
  - Blocker: `python development-tools/bank4/extract_bank4.py` succeeded and regenerated `content/game-data/defense/generated/bank4-defense-play-pointers.json` plus `content/game-data/bank4/generated/summary.json`, but `dotnet build` cannot run because `/home/montblanc/repos/football-game` currently contains no `.sln` or `.csproj` file. Evidence: `find . -maxdepth 2 \( -name '*.sln' -o -name '*.csproj' \) | sort` returned no results, and `dotnet build` failed with `MSB1003: Specify a project or solution file. The current working directory does not contain a project or solution file.` The Bank4 working tree is coherent enough for review, but I did not commit or push because the required validation gate is structurally blocked.
- [x] Decide the default policy for consulting or reusing code from the old MonoGame repo. Acceptance: one short note states whether the default is no reuse, selective packet-by-packet reuse, or broader reuse only with explicit source-bank justification.
  - Evidence: added `docs/planning/old-code-reuse-policy.md`, which sets the default to **no reuse by default** and requires any later reuse to be justified packet-by-packet against a named source-bank responsibility.
- [x] Turn the assembly-first planning docs into a clean restart sequence. Acceptance: one short durable note orders the first banks/packets to tackle from scratch.
  - Evidence: added `docs/planning/restart-sequence.md`, which orders the fresh-start conversion through a Phase 1 gameplay-parity backbone (`Bank1_2`, `5A`, `5C`, `21A`, `17A`, `19A`, `12A`) before widening into playcall, post-play, stats, collision, and later meta-state slices.
- [x] Choose the first bounded conversion packet for actual implementation. Acceptance: the task names the packet id, the source bank, the expected output/artifact, and whether old code may be consulted.
  - Evidence: added `docs/planning/first-conversion-packet.md`, which selects packet `5A` from `Bank5_6_off_def_play_data.asm`, defines the expected packet-analysis note + initial modern data artifact + validation note, and explicitly says old MonoGame code should **not** be consulted for this first packet.
- [x] Capture `Bank1_2_team_data.asm` data semantics needed by packet `5A`. Acceptance: leave one durable note naming the exact team/player data structures or enums that `5A` depends on, with any unresolved semantics called out explicitly.
  - Evidence: added `docs/planning/packets/5A-team-data-semantics.md`, which traces `OFFENSE_PLAYER_REACTION_091`'s `PLAYER_COMMAND_DATA_RB2` / `PLAYER_COMMAND_DATA_RB1` targets back to the shared player-command slot enum in `macros/play_data_macros.asm`, the per-team roster pointer order in `Bank1_2_team_data.asm` (`STARTING_ADDR_FOR_TEAM_PLAYER_NAMES_PTR_TABLE`, `BUFFALO_LIST` as the concrete layout example), and the aligned per-team ability blocks (`BUFFALO_BILLS_ABILITIES`). The note explicitly calls out unresolved semantics around exact rating-byte decoding and where runtime consumption of those ratings is handled.

- [x] Convert `Bank1_2_team_data.asm` end-to-end as the first full-bank pass. Acceptance: the repo gains (a) a durable Bank1_2 structure note, (b) source-faithful extracted artifacts for team roster ordering and player ability layout, (c) decoded C# semantic models for roster slots, player identity, and position-group ability records, and (d) a short validation note explaining how the converted artifacts map back to the bank sections.
  - Evidence: added `docs/planning/banks/Bank1_2-structure-and-representation.md`, `content/game-data/teams/roster-ordering.yaml`, `content/game-data/teams/ability-layout.yaml`, `src/FootballGame/GameData/Teams/Models/*.cs`, `src/FootballGame/GameData/Teams/TeamDataSamples.cs`, and `docs/planning/validation/Bank1_2-conversion-validation.md`.
- [x] Define the Bank1_2 source-data representation strategy before broad extraction. Acceptance: one durable note states which Bank1_2 structures remain source-shaped, which are decoded semantically, and why pointer mechanics are not preserved literally in C#.
  - Evidence: `docs/planning/banks/Bank1_2-structure-and-representation.md` explicitly defines the source-faithful extracted layer, decoded semantic layer, runtime-consumption layer, and the rule to preserve pointer semantics without carrying pointer mechanics into C#.
- [x] Extract the full canonical roster-slot ordering and team/player identity layer from Bank1_2. Acceptance: one reviewable artifact preserves all teams, slot order, jersey numbers, and source names without flattening away the bank’s ordering semantics.
  - Evidence: `content/game-data/teams/roster-ordering.yaml` preserves canonical team/slot order, `development-tools/bank1_2/extract_bank1_2.py` generates `content/game-data/teams/generated/team-identities.json`, and the generated output covers all 28 teams × 30 slots = 840 identity records. `PlayerIdentityRecord` / `TeamRosterRecord` remain the decoded semantic layer.
- [x] Extract and decode the full Bank1_2 ability layer by position-group schema. Acceptance: one reviewable artifact plus typed C# records preserve the per-position record widths, nibble-based attribute scale, and aligned per-team ability blocks.
  - Evidence: `content/game-data/teams/ability-layout.yaml` preserves the slot-aligned ability contract, `development-tools/bank1_2/extract_bank1_2.py` generates `content/game-data/teams/generated/team-abilities.json` plus `ability-metadata.json`, `AttributeGrade` preserves nibble semantics, and `AbilityRecords.cs` / `TeamAbilitySet.cs` provide the decoded typed records across all 28 team ability blobs / 840 slot records.
- [x] Keep packet `5A` aligned with the full-bank Bank1_2 conversion. Acceptance: reconcile `docs/planning/packets/5A-team-data-semantics.md` and `src/FootballGame/Conversion/PlayScripts/OffensivePitchPlayFamily.cs` against the final Bank1_2 models without consulting the older MonoGame repo.
  - Evidence: the existing `5A` team-data note now matches the canonical `RosterSlot` / `TeamId` / Bank1_2 ability-schema direction introduced in the new Bank1_2 models, and no old-repo code was consulted.
- [x] Add a loader/decoder bridge from generated Bank1_2 artifacts into the semantic C# models. Acceptance: one loader reads the generated Bank1_2 JSON artifacts and returns typed roster/ability data without reintroducing pointer mechanics.
  - Evidence: added `src/FootballGame/GameData/Teams/TeamDataJsonLoader.cs`, `src/FootballGame/GameData/Teams/TeamDataSet.cs`, and `docs/planning/banks/Bank1_2-loader-layer.md`.
- [x] Convert `Bank3_formation_metatile_data.asm` end-to-end as the next full-bank pass. Acceptance: the repo gains (a) a durable Bank3 structure note, (b) source-faithful extracted artifacts for formation pointer tables, offensive execution tables, and metatile pointer/data blocks, (c) decoded C# semantic models for formation families and background/metatile layout records, and (d) a short validation note explaining how the converted artifacts map back to the bank sections without consulting the old MonoGame repo.
  - Evidence: added `docs/planning/banks/Bank3-structure-and-representation.md`, `development-tools/bank3/extract_bank3.py`, generated `content/game-data/formations/generated/bank3-formations.json`, generated `content/game-data/backgrounds/generated/bank3-metatile-layouts.json`, generated `content/game-data/bank3/generated/summary.json`, typed models under `src/FootballGame/GameData/Formations/Models/` and `src/FootballGame/GameData/Backgrounds/Models/`, and `docs/planning/validation/Bank3-conversion-validation.md`. Validation: `python development-tools/bank3/extract_bank3.py` and a follow-up Python assertion pass confirmed 22 formation tables, 92 offensive execution tables, 16 special offensive-play tables, and the 76-pointer / 75-record metatile layout structure including the duplicated default-helmet pointer alias.
- [x] Convert `Bank4_def_spec_play_pointers_data.asm` end-to-end as the next full-bank pass. Acceptance: the repo gains (a) a durable Bank4 structure note, (b) source-faithful extracted artifacts for defensive execution tables plus special-teams / defense pointer families, (c) decoded C# semantic models for defensive play families that stay aligned with the Bank1_2 roster vocabulary and Bank3 formation/execution layers, and (d) a short validation note explaining how the converted artifacts map back to the bank sections without consulting the old MonoGame repo.
  - Evidence: added `docs/planning/banks/Bank4-structure-and-representation.md`, `development-tools/bank4/extract_bank4.py`, generated `content/game-data/defense/generated/bank4-defense-play-pointers.json`, generated `content/game-data/bank4/generated/summary.json`, typed models under `src/FootballGame/GameData/Defense/Models/`, and `docs/planning/validation/Bank4-conversion-validation.md`. Validation: `python development-tools/bank4/extract_bank4.py` and a follow-up Python assertion pass confirmed 255 defensive execution tables with 11 entries each plus 16 special defense-play tables with 12 entries each.

- [x] Fix the highest-risk Bank19_20 football-rules parity gaps found in the 2026-06-02 review. Acceptance: correct the coordinator’s interception-return dispatch ordering, safety kickoff direction/ownership, and the most source-visible special-teams spot/blocked-play mistake(s); validate the slice with targeted assertions or equivalent evidence.
  - Evidence: updated `OnFieldPlayCoordinator` so turnover returns dispatch before pass-loop routing, safeties now queue a free kick by the safetied team while still awarding possession to the scoring side, blocked punts now stay in live loose-ball/recovery flow, missed field goals stamp a source-directed next-snap spot adjustment, and XP exit no longer fabricates a normal possession change. Validation: targeted Python assertions verified the new coordinator/state/service hooks and `git diff --check` passed.

## Notes
- This task file intentionally avoids carrying forward old completed tasks, old validation campaigns, or old architecture milestones.
- Future tasks should be source-bank-first, not old-code-first.
- All implementation/refactor work should follow `docs/coding-standards.md` and the packet flow in `docs/planning/source-bank-conversion-checklist.md`.
